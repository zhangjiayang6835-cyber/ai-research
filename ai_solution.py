```json
{
  "solution_code": "import threading\nimport time\nfrom decimal import Decimal\nfrom typing import Optional\nfrom contextlib import contextmanager\nfrom dataclasses import dataclass\nfrom enum import Enum\nimport logging\n\nlogger = logging.getLogger(__name__)\n\n\nclass TransactionStatus(Enum):\n    \"\"\"Transaction status enumeration\"\"\"\n    PENDING = \"pending\"\n    SUCCESS = \"success\"\n    FAILED = \"failed\"\n    INSUFFICIENT_FUNDS = \"insufficient_funds\"\n\n\n@dataclass\nclass TransactionResult:\n    \"\"\"Result of a transaction operation\"\"\"\n    status: TransactionStatus\n    transaction_id: str\n    balance_before: Decimal\n    balance_after: Optional[Decimal]\n    message: str\n\n\nclass Account:\n    \"\"\"Account model with optimistic locking support\"\"\"\n    \n    def __init__(self, account_id: str, balance: Decimal, version: int = 0):\n        self.account_id = account_id\n        self.balance = balance\n        self.version = version\n        self.lock = threading.Lock()\n    \n    def to_dict(self):\n        return {\n            \"account_id\": self.account_id,\n            \"balance\": float(self.balance),\n            \"version\": self.version\n        }\n\n\nclass AccountRepository:\n    \"\"\"Repository for account persistence with row-level locking simulation\"\"\"\n    \n    def __init__(self):\n        self._accounts = {}\n        self._global_lock = threading.Lock()\n    \n    def create_account(self, account_id: str, initial_balance: Decimal) -> Account:\n        \"\"\"Create a new account\"\"\"\n        with self._global_lock:\n            if account_id in self._accounts:\n                raise ValueError(f\"Account {account_id} already exists\")\n            account = Account(account_id, initial_balance)\n            self._accounts[account_id] = account\n            return account\n    \n    def get_account_for_update(self, account_id: str) -> Optional[Account]:\n        \"\"\"Get account with row-level lock (SELECT FOR UPDATE simulation)\"\"\"\n        with self._global_lock:\n            account = self._accounts.get(account_id)\n            if account:\n                # Acquire row-level lock\n                account.lock.acquire()\n            return account\n    \n    def release_lock(self, account: Account):\n        \"\"\"Release row-level lock\"\"\"\n        if account.lock.locked():\n            account.lock.release()\n    \n    def update_account_optimistic(self, account: Account, new_balance: Decimal) -> bool:\n        \"\"\"Update account with optimistic locking (version check)\n        \n        Returns:\n            True if update successful, False if version mismatch\n        \"\"\"\n        with self._global_lock:\n            current_account = self._accounts.get(account.account_id)\n            if not current_account:\n                return False\n            \n            # Check version for optimistic locking\n            if current_account.version != account.version:\n                logger.warning(\n                    f\"Version mismatch for account {account.account_id}: \"\n                    f\"expected {account.version}, got {current_account.version}\"\n                )\n                return False\n            \n            # Update balance and increment version\n            current_account.balance = new_balance\n            current_account.version += 1\n            return True\n    \n    def get_balance(self, account_id: str) -> Optional[Decimal]:\n        \"\"\"Get current balance (non-locking read)\"\"\"\n        account = self._accounts.get(account_id)\n        return account.balance if account else None\n\n\nclass DistributedPaymentService:\n    \"\"\"Payment service with race condition protection\n    \n    Implements three-layer protection:\n    1. Database transaction with row-level locking (SELECT FOR UPDATE)\n    2. Optimistic locking with version numbers\n    3. Post-debit balance validation\n    \"\"\"\n    \n    def __init__(self, repository: AccountRepository, max_retries: int = 3):\n        self.repository = repository\n        self.max_retries = max_retries\n        self.transaction_counter = 0\n        self.counter_lock = threading.Lock()\n    \n    def _generate_transaction_id(self) -> str:\n        \"\"\"Generate unique transaction ID\"\"\"\n        with self.counter_lock:\n            self.transaction_counter += 1\n            return f\"TXN-{int(time.time() * 1000)}-{self.transaction_counter}\"\n    \n    @contextmanager\n    def _transaction_scope(self, account: Account):\n        \"\"\"Context manager for transaction with automatic rollback\"\"\"\n        try:\n            yield account\n        except Exception as e:\n            logger.error(f\"Transaction failed: {e}\")\n            raise\n        finally:\n            # Release row-level lock\n            self.repository.release_lock(account)\n    \n    def debit_account_pessimistic(self, account_id: str, amount: Decimal) -> TransactionResult:\n        \"\"\"Debit account using pessimistic locking (SELECT FOR UPDATE)\n        \n        This approach acquires a row-level lock before checking balance,\n        preventing concurrent modifications.\n        \n        Args:\n            account_id: Account identifier\n            amount: Amount to debit (must be positive)\n        \n        Returns:\n            TransactionResult with operation status\n        \"\"\"\n        if amount <= 0:\n            return TransactionResult(\n                status=TransactionStatus.FAILED,\n                transaction_id=self._generate_transaction_id(),\n                balance_before=Decimal(0),\n                balance_after=None,\n                message=\"Amount must be positive\"\n            )\n        \n        transaction_id = self._generate_transaction_id()\n        \n        # Acquire row-level lock (SELECT FOR UPDATE)\n        account = self.repository.get_account_for_update(account_id)\n        \n        if not account:\n            return TransactionResult(\n                status=TransactionStatus.FAILED,\n                transaction_id=transaction_id,\n                balance_before=Decimal(0),\n                balance_after=None,\n                message=f\"Account {account_id} not found\"\n            )\n        \n        try:\n            with self._transaction_scope(account):\n                balance_before = account.balance\n                \n                # Check sufficient funds\n                if account.balance < amount:\n                    logger.warning(\n                        f\"[{transaction_id}] Insufficient funds: \"\n                        f\"balance={account.balance}, amount={amount}\"\n                    )\n                    return TransactionResult(\n                        status=TransactionStatus.INSUFFICIENT_FUNDS,\n                        transaction_id=transaction_id,\n                        balance_before=balance_before,\n                        balance_after=account.balance,\n                        message=f\"Insufficient funds: have {account.balance}, need {amount}\"\n                    )\n                \n                # Perform debit\n                new_balance = account.balance - amount\n                \n                # Critical validation: ensure balance is non-negative after debit\n                if new_balance < 0:\n                    logger.error(\n                        f\"[{transaction_id}] Balance would be negative after debit: {new_balance}\"\n                    )\n                    return TransactionResult(\n                        status=TransactionStatus.FAILED,\n                        transaction_id=transaction_id,\n                        balance_before=balance_before,\n                        balance_after=account.balance,\n                        message=\"Transaction would result in negative balance\"\n                    )\n                \n                # Update balance using optimistic lock\n                success = self.repository.update_account_optimistic(account, new_balance)\n                \n                if not success:\n                    logger.error(f\"[{transaction_id}] Failed to update account due to version mismatch\")\n                    return TransactionResult(\n                        status=TransactionStatus.FAILED,\n                        transaction_id=transaction_id,\n                        balance_before=balance_before,\n                        balance_after=account.balance,\n                        message=\"Concurrent modification detected, please retry\"\n                    )\n                \n                logger.info(\n                    f\"[{transaction_id}] Debit successful: \"\n                    f\"{balance_before} -> {new_balance} (amount={amount})\"\n                )\n                \n                return TransactionResult(\n                    status=TransactionStatus.SUCCESS,\n                    transaction_id=transaction_id,\n                    balance_before=balance_before,\n                    balance_after=new_balance,\n                    message=\"Transaction successful\"\n                )\n        \n        except Exception as e:\n            logger.error(f\"[{transaction_id}] Transaction error: {e}\")\n            return TransactionResult(\n                status=TransactionStatus.FAILED,\n                transaction_id=transaction_id,\n                balance_before=account.balance,\n                balance_after=None,\n                message=f\"Transaction failed: {str(e)}\"\n            )\n    \n    def debit_account_optimistic(self, account_id: str, amount: Decimal) -> TransactionResult:\n        \"\"\"Debit account using optimistic locking with retry\n        \n        This approach checks version numbers and retries on conflict.\n        Suitable for low-contention scenarios.\n        \n        Args:\n            account_id: Account identifier\n            amount: Amount to
```json
{
  "solution_code": "\"\"\"Secure encryption module using AES-GCM for authenticated encryption.\n\nThis module replaces the insecure ECB mode with AES-GCM (Galois/Counter Mode),\nwhich provides both confidentiality and authenticity.\n\"\"\"\n\nimport os\nfrom typing import Tuple\nfrom cryptography.hazmat.primitives.ciphers.aead import AESGCM\nfrom cryptography.exceptions import InvalidTag\nimport base64\n\n\nclass SecureEncryption:\n    \"\"\"Secure encryption using AES-GCM with random IV generation.\"\"\"\n\n    def __init__(self, key: bytes = None):\n        \"\"\"\n        Initialize encryption with a key.\n        \n        Args:\n            key: 32-byte encryption key. If None, generates a new key.\n        \"\"\"\n        if key is None:\n            key = AESGCM.generate_key(bit_length=256)\n        elif len(key) not in [16, 24, 32]:\n            raise ValueError(\"Key must be 16, 24, or 32 bytes long\")\n        \n        self.key = key\n        self.aesgcm = AESGCM(self.key)\n\n    @staticmethod\n    def generate_key() -> bytes:\n        \"\"\"Generate a new 256-bit encryption key.\n        \n        Returns:\n            32-byte random encryption key\n        \"\"\"\n        return AESGCM.generate_key(bit_length=256)\n\n    def encrypt(self, plaintext: bytes, associated_data: bytes = None) -> Tuple[bytes, bytes]:\n        \"\"\"\n        Encrypt data using AES-GCM with a random nonce.\n        \n        Args:\n            plaintext: Data to encrypt\n            associated_data: Optional additional authenticated data (AAD)\n        \n        Returns:\n            Tuple of (nonce, ciphertext). The nonce must be stored with the ciphertext.\n        \"\"\"\n        # Generate a random 96-bit (12-byte) nonce for GCM\n        nonce = os.urandom(12)\n        \n        # Encrypt and authenticate\n        ciphertext = self.aesgcm.encrypt(nonce, plaintext, associated_data)\n        \n        return nonce, ciphertext\n\n    def decrypt(self, nonce: bytes, ciphertext: bytes, associated_data: bytes = None) -> bytes:\n        \"\"\"\n        Decrypt and verify data using AES-GCM.\n        \n        Args:\n            nonce: The nonce used during encryption\n            ciphertext: Encrypted data to decrypt\n            associated_data: Optional AAD (must match encryption AAD)\n        \n        Returns:\n            Decrypted plaintext\n        \n        Raises:\n            InvalidTag: If authentication fails (data has been tampered with)\n        \"\"\"\n        try:\n            plaintext = self.aesgcm.decrypt(nonce, ciphertext, associated_data)\n            return plaintext\n        except InvalidTag:\n            raise ValueError(\"Decryption failed: Data has been tampered with or key is incorrect\")\n\n    def encrypt_to_base64(self, plaintext: str, associated_data: str = None) -> str:\n        \"\"\"\n        Convenience method to encrypt a string and return base64-encoded result.\n        \n        Args:\n            plaintext: String to encrypt\n            associated_data: Optional additional authenticated data\n        \n        Returns:\n            Base64-encoded string containing nonce + ciphertext\n        \"\"\"\n        plaintext_bytes = plaintext.encode('utf-8')\n        aad_bytes = associated_data.encode('utf-8') if associated_data else None\n        \n        nonce, ciphertext = self.encrypt(plaintext_bytes, aad_bytes)\n        \n        # Combine nonce and ciphertext for storage\n        combined = nonce + ciphertext\n        return base64.b64encode(combined).decode('utf-8')\n\n    def decrypt_from_base64(self, encrypted_data: str, associated_data: str = None) -> str:\n        \"\"\"\n        Convenience method to decrypt a base64-encoded encrypted string.\n        \n        Args:\n            encrypted_data: Base64-encoded nonce + ciphertext\n            associated_data: Optional AAD (must match encryption AAD)\n        \n        Returns:\n            Decrypted string\n        \"\"\"\n        combined = base64.b64decode(encrypted_data.encode('utf-8'))\n        \n        # Extract nonce (first 12 bytes) and ciphertext (rest)\n        nonce = combined[:12]\n        ciphertext = combined[12:]\n        \n        aad_bytes = associated_data.encode('utf-8') if associated_data else None\n        \n        plaintext_bytes = self.decrypt(nonce, ciphertext, aad_bytes)\n        return plaintext_bytes.decode('utf-8')\n\n\n# Example usage for user data encryption\nclass UserDataEncryption:\n    \"\"\"Helper class for encrypting user data with role information.\"\"\"\n\n    def __init__(self, encryption_key: bytes = None):\n        \"\"\"\n        Initialize user data encryption.\n        \n        Args:\n            encryption_key: Encryption key. If None, loads from environment or generates new.\n        \"\"\"\n        if encryption_key is None:\n            # In production, load from secure key management system\n            encryption_key = os.environ.get('ENCRYPTION_KEY')\n            if encryption_key:\n                encryption_key = base64.b64decode(encryption_key)\n            else:\n                # Generate and warn - in production, this should be persistent\n                encryption_key = SecureEncryption.generate_key()\n                print(\"WARNING: Generated new encryption key. Set ENCRYPTION_KEY environment variable.\")\n                print(f\"ENCRYPTION_KEY={base64.b64encode(encryption_key).decode('utf-8')}\")\n        \n        self.encryptor = SecureEncryption(encryption_key)\n\n    def encrypt_user_data(self, user_data: str, user_id: str = None) -> str:\n        \"\"\"\n        Encrypt user data with optional user_id as additional authenticated data.\n        \n        Args:\n            user_data: User data to encrypt (e.g., role, permissions)\n            user_id: Optional user identifier for additional authentication\n        \n        Returns:\n            Base64-encoded encrypted data\n        \"\"\"\n        return self.encryptor.encrypt_to_base64(user_data, user_id)\n\n    def decrypt_user_data(self, encrypted_data: str, user_id: str = None) -> str:\n        \"\"\"\n        Decrypt user data and verify authenticity.\n        \n        Args:\n            encrypted_data: Base64-encoded encrypted data\n            user_id: Optional user identifier (must match encryption)\n        \n        Returns:\n            Decrypted user data\n        \n        Raises:\n            ValueError: If decryption or authentication fails\n        \"\"\"\n        return self.encryptor.decrypt_from_base64(encrypted_data, user_id)\n",
  "file_hints": [
    "src/utils/encryption.py",
    "app/security/encryption.py",
    "lib/crypto/secure_encryption.py",
    "backend/utils/encryption.py"
  ],
  "pr_title": "[SECURITY] Replace ECB encryption with AES-GCM authenticated encryption",
  "pr_body": "## 🔒 Security Fix: ECB Mode → AES-GCM\n\n### Problem\nThe application was using **AES-ECB mode** for encrypting user data, which has a critical security vulnerability:\n- ECB produces **identical ciphertext for identical plaintext blocks**\n- Attackers can use **pattern matching** to identify sensitive information (e.g., \"admin\" vs \"user\" roles)\n- No authentication - data can be tampered with undetected\n\n### Solution\nReplaced ECB with **AES-GCM (Galois/Counter Mode)**, which provides:\n- ✅ **Authenticated Encryption with Associated Data (AEAD)**\n- ✅ **Random IV (nonce) generation** for each encryption\n- ✅ **Pattern resistance** - same plaintext produces different ciphertext\n- ✅ **Tamper detection** - fails if data is modified\n\n### Changes\n1. **New `SecureEncryption` class** using `cryptography` library's AES-GCM\n2. **Random 96-bit nonce** generated for each encryption operation\n3. **Optional AAD support** for binding encrypted data to context (e.g., user ID)\n4. **Convenience methods** for base64 encoding/decoding\n5. **UserDataEncryption helper** for common use cases\n\n### Security Features\n- 🔐 256-bit AES-GCM encryption\n- 🎲 Cryptographically random nonce per operation\n- ✓ Built-in authentication tag (16 bytes)\n- 🛡️ Tamper-evident (raises `ValueError` on modification)\n\n### Testing\n```python\nfrom encryption import UserDataEncryption\n\n# Initialize\nencryptor = UserDataEncryption()\n\n# Encrypt user role\nencrypted = encryptor.encrypt_user_data(\"admin\", user_id=\"user123\")\nprint(f\"Encrypted: {encrypted}\")\n\n# Decrypt\ndecrypted = encryptor.decrypt_user_data(encrypted, user_id=\"user123\")\nassert decrypted == \"admin\"\n\n# Verify same plaintext produces different
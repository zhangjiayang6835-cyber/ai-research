```json
{
  "solution_code": "import socket\nimport ipaddress\nimport urllib.parse\nfrom typing import Optional, Tuple\nimport dns.resolver\nimport requests\nfrom requests.adapters import HTTPAdapter\nfrom urllib3.util.retry import Retry\nimport time\n\n\nclass SSRFProtectionError(Exception):\n    \"\"\"Custom exception for SSRF protection violations\"\"\"\n    pass\n\n\nclass DNSRebindingProtectedHTTPAdapter(HTTPAdapter):\n    \"\"\"\n    Custom HTTP adapter that prevents DNS rebinding attacks by:\n    1. Forcing DNS resolution on every request\n    2. Validating resolved IPs against private ranges\n    3. Preventing redirects to private IPs\n    \"\"\"\n    \n    def __init__(self, *args, **kwargs):\n        self.dns_cache_ttl = 0  # Force DNS refresh on every request\n        super().__init__(*args, **kwargs)\n    \n    def send(self, request, **kwargs):\n        # Parse the URL to get the hostname\n        parsed_url = urllib.parse.urlparse(request.url)\n        hostname = parsed_url.hostname\n        \n        if not hostname:\n            raise SSRFProtectionError(\"Invalid URL: no hostname found\")\n        \n        # Force fresh DNS resolution and validate\n        try:\n            resolved_ips = self._resolve_and_validate(hostname)\n        except SSRFProtectionError:\n            raise\n        except Exception as e:\n            raise SSRFProtectionError(f\"DNS resolution failed: {str(e)}\")\n        \n        # Proceed with the request\n        return super().send(request, **kwargs)\n    \n    def _resolve_and_validate(self, hostname: str) -> list:\n        \"\"\"\n        Resolve hostname and validate all IPs are not in private ranges.\n        Forces fresh DNS query by bypassing system cache.\n        \"\"\"\n        resolver = dns.resolver.Resolver()\n        resolver.cache = None  # Disable caching\n        resolver.lifetime = 5.0  # 5 second timeout\n        \n        resolved_ips = []\n        \n        # Try to resolve both A and AAAA records\n        for record_type in ['A', 'AAAA']:\n            try:\n                answers = resolver.resolve(hostname, record_type)\n                for rdata in answers:\n                    ip_str = str(rdata)\n                    resolved_ips.append(ip_str)\n                    \n                    # Validate each resolved IP\n                    if not self._is_safe_ip(ip_str):\n                        raise SSRFProtectionError(\n                            f\"Blocked private/reserved IP address: {ip_str} for hostname {hostname}\"\n                        )\n            except dns.resolver.NoAnswer:\n                continue\n            except dns.resolver.NXDOMAIN:\n                raise SSRFProtectionError(f\"Domain does not exist: {hostname}\")\n            except SSRFProtectionError:\n                raise\n            except Exception:\n                continue\n        \n        if not resolved_ips:\n            raise SSRFProtectionError(f\"No valid IP addresses found for hostname: {hostname}\")\n        \n        return resolved_ips\n    \n    def _is_safe_ip(self, ip_str: str) -> bool:\n        \"\"\"\n        Check if an IP address is safe (not in private/reserved ranges).\n        Returns True if safe, False if dangerous.\n        \"\"\"\n        try:\n            ip = ipaddress.ip_address(ip_str)\n            \n            # Block private IP ranges\n            if ip.is_private:\n                return False\n            \n            # Block loopback\n            if ip.is_loopback:\n                return False\n            \n            # Block link-local (169.254.0.0/16 - AWS metadata)\n            if ip.is_link_local:\n                return False\n            \n            # Block multicast\n            if ip.is_multicast:\n                return False\n            \n            # Block reserved\n            if ip.is_reserved:\n                return False\n            \n            # Additional check for AWS metadata IP\n            if ip_str == '169.254.169.254':\n                return False\n            \n            # Block localhost variations\n            if ip_str in ['127.0.0.1', '::1', '0.0.0.0', '::']:\n                return False\n            \n            return True\n            \n        except ValueError:\n            return False\n\n\nclass SSRFProtectedSession:\n    \"\"\"\n    HTTP session with comprehensive SSRF protection:\n    - DNS rebinding protection\n    - Private IP blocking\n    - Redirect limiting\n    - Forced DNS refresh\n    \"\"\"\n    \n    def __init__(\n        self,\n        max_redirects: int = 0,  # Default: no redirects allowed\n        timeout: int = 10,\n        max_retries: int = 0\n    ):\n        self.max_redirects = max_redirects\n        self.timeout = timeout\n        self.session = requests.Session()\n        \n        # Configure adapter with DNS rebinding protection\n        adapter = DNSRebindingProtectedHTTPAdapter(\n            max_retries=Retry(\n                total=max_retries,\n                read=max_retries,\n                connect=max_retries,\n                backoff_factor=0.3\n            )\n        )\n        \n        self.session.mount('http://', adapter)\n        self.session.mount('https://', adapter)\n    \n    def request(\n        self,\n        method: str,\n        url: str,\n        validate_response: bool = True,\n        **kwargs\n    ) -> requests.Response:\n        \"\"\"\n        Make an HTTP request with SSRF protection.\n        \n        Args:\n            method: HTTP method (GET, POST, etc.)\n            url: Target URL\n            validate_response: Whether to validate response for redirect attempts\n            **kwargs: Additional arguments passed to requests\n        \n        Returns:\n            Response object\n        \n        Raises:\n            SSRFProtectionError: If SSRF protection is triggered\n        \"\"\"\n        # Disable redirects at the request level\n        kwargs['allow_redirects'] = False\n        kwargs.setdefault('timeout', self.timeout)\n        \n        try:\n            # Make initial request\n            response = self.session.request(method, url, **kwargs)\n            \n            # Handle redirects manually with validation\n            redirect_count = 0\n            while response.is_redirect and redirect_count < self.max_redirects:\n                redirect_count += 1\n                \n                # Get redirect location\n                redirect_url = response.headers.get('Location')\n                if not redirect_url:\n                    break\n                \n                # Make absolute URL\n                redirect_url = urllib.parse.urljoin(url, redirect_url)\n                \n                # Validate redirect URL before following\n                parsed = urllib.parse.urlparse(redirect_url)\n                if parsed.hostname:\n                    # Force DNS validation on redirect target\n                    adapter = self.session.get_adapter(redirect_url)\n                    if isinstance(adapter, DNSRebindingProtectedHTTPAdapter):\n                        adapter._resolve_and_validate(parsed.hostname)\n                \n                # Follow redirect\n                url = redirect_url\n                response = self.session.request(method, url, **kwargs)\n            \n            # Validate final response if needed\n            if validate_response:\n                self._validate_response_content(response)\n            \n            return response\n            \n        except requests.exceptions.RequestException as e:\n            raise SSRFProtectionError(f\"Request failed: {str(e)}\")\n    \n    def get(self, url: str, **kwargs) -> requests.Response:\n        \"\"\"Make a GET request with SSRF protection\"\"\"\n        return self.request('GET', url, **kwargs)\n    \n    def post(self, url: str, **kwargs) -> requests.Response:\n        \"\"\"Make a POST request with SSRF protection\"\"\"\n        return self.request('POST', url, **kwargs)\n    \n    def _validate_response_content(self, response: requests.Response) -> None:\n        \"\"\"\n        Validate response content for potential SSRF indicators.\n        Can be extended to check for cloud metadata patterns.\n        \"\"\"\n        # Check for suspicious headers that might indicate metadata service\n        suspicious_headers = [\n            'x-aws-ec2-metadata-token',\n            'x-gcp-metadata',\n            'metadata-flavor'\n        ]\n        \n        for header in suspicious_headers:\n            if header.lower() in [h.lower() for h in response.headers.keys()]:\n                raise SSRFProtectionError(\n                    f\"Response contains suspicious metadata header: {header}\"\n                )\n    \n    def close(self):\n        \"\"\"Close the session\"\"\"\n        self.session.close()\n    \n    def __enter__(self):\n        return self\n    \n    def __exit__(self, *args):\n        self.close()\n\n\n# Convenience functions\ndef safe_get(url: str, max_redirects: int = 0, timeout: int = 10, **kwargs) -> requests.Response:\n    \"\"\"\n    Perform a safe GET request with SSRF protection.\n    \n    Args:\n        url: Target URL\n        max_redirects: Maximum number of redirects to follow (default: 0)\n        timeout: Request timeout in seconds\n        **kwargs: Additional arguments passed to requests\n    \n    Returns:\n        Response object\n    